# CSS Frameworks CA - Social app

A static social media app built to practice styling with Bootstrap and Sass, as part of the CSS Frameworks course assignment.

This project uses static placeholder content instead of live data from an API, since the assignment requires no JavaScript functionality beyond the mobile menu toggle.
